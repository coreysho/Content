// Market Guard - https://oldschool.runescape.wiki/w/Market_Guard

[ai_queue3,farming_market_guard] @farming_market_guard_drop_table;
[ai_queue3,viking_guard] @farming_market_guard_drop_table;

[label,farming_market_guard_drop_table]
gosub(npc_death);
if (npc_findhero = ^false) {
    return;
}

// Bones / ashes, set on the npc config.
obj_add(npc_coord, npc_param(death_drop), 1, ^lootdrop_duration);

~trail_mediumcluedrop(^trail_medium_droprate, npc_coord);

def_int $dropint = random(128);
if ($dropint < 42) {
    obj_add(npc_coord, coins, 15, ^lootdrop_duration);
} else if ($dropint < 51) {
    obj_add(npc_coord, coins, 22, ^lootdrop_duration);
} else if ($dropint < 56) {
    obj_add(npc_coord, coins, 17, ^lootdrop_duration);
} else if ($dropint < 59) {
    obj_add(npc_coord, coins, 27, ^lootdrop_duration);
} else if ($dropint < 65) {
    obj_add(npc_coord, iron_warhammer, 1, ^lootdrop_duration);
} else if ($dropint < 68) {
    obj_add(npc_coord, bronze_warhammer, 1, ^lootdrop_duration);
} else if ($dropint < 69) {
    obj_add(npc_coord, steel_warhammer, 1, ^lootdrop_duration);
} else if ($dropint < 70) {
    obj_add(npc_coord, black_warhammer, 1, ^lootdrop_duration);
} else if ($dropint < 71) {
    obj_add(npc_coord, viking_helmet, 1, ^lootdrop_duration);
} else if ($dropint < 72) {
    obj_add(npc_coord, viking_sword, 1, ^lootdrop_duration);
} else if ($dropint < 73) {
    obj_add(npc_coord, viking_shield, 1, ^lootdrop_duration);
} else if ($dropint < 77) {
    obj_add(npc_coord, keg_of_beer, 1, ^lootdrop_duration);
} else if ($dropint < 81) {
    obj_add(npc_coord, cake, 1, ^lootdrop_duration);
} else if ($dropint < 84) {
    obj_add(npc_coord, iron_ore, 2, ^lootdrop_duration);
} else if ($dropint < 86) {
    obj_add(npc_coord, copper_ore, 5, ^lootdrop_duration);
} else if ($dropint < 87) {
    obj_add(npc_coord, tin_ore, 1, ^lootdrop_duration);
} else if ($dropint < 88) {
    obj_add(npc_coord, bucket_water, 1, ^lootdrop_duration);
} else if ($dropint < 89) {
    obj_add(npc_coord, fur, 1, ^lootdrop_duration);
} else if ($dropint < 90) {
    obj_add(npc_coord, raw_sardine, 1, ^lootdrop_duration);
} else if ($dropint < 91) {
    obj_add(npc_coord, beer, 1, ^lootdrop_duration);
} else if ($dropint < 92) {
    obj_add(npc_coord, ring_mould, 1, ^lootdrop_duration);
} else if ($dropint < 94) {
    obj_add(npc_coord, ~randomherb, ^lootdrop_duration);
} else if ($dropint < 95) {
    obj_add(npc_coord, ~ultrarare_getitem, ^lootdrop_duration);
}
