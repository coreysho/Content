// Mourner - https://oldschool.runescape.wiki/w/Mourner

[ai_queue3,mourner2] @mourner2_drop_table;
[ai_queue3,mourner3] @mourner2_drop_table;
[ai_queue3,mourner_armed] @mourner2_drop_table;
[ai_queue3,mourner_hideout_guard] @mourner2_drop_table;
[ai_queue3,mournerstew1] @mourner2_drop_table;
[ai_queue3,mournerstew3] @mourner2_drop_table;
// The three overpass mourners of Mourning's End Part I go to their own table: they drop the BLOODY
// top and the RIPPED trousers rather than the clean pair, because the soap-and-seamstress chain of
// that quest's first half hangs off them. Everything else about the drop is identical, so the two
// tables share their random half.
[ai_queue3,mourning_overpass_mourner1] @mourning_overpass_drop_table;
[ai_queue3,mourning_overpass_mourner2] @mourning_overpass_drop_table;
[ai_queue3,mourning_overpass_mourner3] @mourning_overpass_drop_table;

[label,mourner2_drop_table]
gosub(npc_death);
if (npc_findhero = ^false) {
    return;
}

// Bones / ashes, set on the npc config. FIXED 2026-09-08: this was immediately followed by a
// second, unconditional obj_add of bones, so every mourner in the game dropped two sets. None of
// the npcs on this table overrides death_drop, and its default is bones, so the explicit line was
// pure duplication.
obj_add(npc_coord, npc_param(death_drop), 1, ^lootdrop_duration);

obj_add(npc_coord, mourning_mourner_top, 1, ^lootdrop_duration);
obj_add(npc_coord, mourning_mourner_legs, 1, ^lootdrop_duration);
obj_add(npc_coord, mourning_mourner_cloak, 1, ^lootdrop_duration);
obj_add(npc_coord, mourning_mourner_boots, 1, ^lootdrop_duration);
obj_add(npc_coord, mourning_mourner_gloves, 1, ^lootdrop_duration);
obj_add(npc_coord, gasmask, 1, ^lootdrop_duration);
@mourner_common_drops;

// As above, but with the damaged uniform. Both tables end by jumping into the shared half below.
[label,mourning_overpass_drop_table]
gosub(npc_death);
if (npc_findhero = ^false) {
    return;
}
obj_add(npc_coord, npc_param(death_drop), 1, ^lootdrop_duration);
obj_add(npc_coord, mourning_bloody_mourner_top, 1, ^lootdrop_duration);
obj_add(npc_coord, mourning_ripped_mourner_legs, 1, ^lootdrop_duration);
obj_add(npc_coord, mourning_mourner_cloak, 1, ^lootdrop_duration);
obj_add(npc_coord, mourning_mourner_boots, 1, ^lootdrop_duration);
obj_add(npc_coord, mourning_mourner_gloves, 1, ^lootdrop_duration);
obj_add(npc_coord, gasmask, 1, ^lootdrop_duration);
@mourner_common_drops;

// The half both tables share. npc_findhero has already been called and has already returned true
// in either caller, so active_player is set by the time control reaches here.
[label,mourner_common_drops]
~trail_hardcluedrop(^trail_hard_droprate, npc_coord);

def_int $dropint = random(128);
if ($dropint < 4) {
    obj_add(npc_coord, dragonhide_body, 1, ^lootdrop_duration);
} else if ($dropint < 7) {
    obj_add(npc_coord, dragonhide_chaps, 1, ^lootdrop_duration);
} else if ($dropint < 9) {
    obj_add(npc_coord, mithril_spear, 1, ^lootdrop_duration);
} else if ($dropint < 10) {
    obj_add(npc_coord, mithril_kiteshield, 1, ^lootdrop_duration);
} else if ($dropint < 11) {
    obj_add(npc_coord, adamant_full_helm, 1, ^lootdrop_duration);
} else if ($dropint < 12) {
    obj_add(npc_coord, rune_dagger, 1, ^lootdrop_duration);
} else if ($dropint < 20) {
    obj_add(npc_coord, waterrune, 70, ^lootdrop_duration);
} else if ($dropint < 25) {
    obj_add(npc_coord, naturerune, 12, ^lootdrop_duration);
} else if ($dropint < 28) {
    obj_add(npc_coord, lawrune, 2, ^lootdrop_duration);
} else if ($dropint < 29) {
    obj_add(npc_coord, firerune, 37, ^lootdrop_duration);
} else if ($dropint < 44) {
    obj_add(npc_coord, ~randomherb, ^lootdrop_duration);
} else if ($dropint < 73) {
    obj_add(npc_coord, coins, 44, ^lootdrop_duration);
} else if ($dropint < 98) {
    obj_add(npc_coord, coins, 132, ^lootdrop_duration);
} else if ($dropint < 108) {
    obj_add(npc_coord, coins, 180, ^lootdrop_duration);
} else if ($dropint < 113) {
    obj_add(npc_coord, coins, 20, ^lootdrop_duration);
} else if ($dropint < 114) {
    obj_add(npc_coord, coins, 440, ^lootdrop_duration);
} else if ($dropint < 117) {
    obj_add(npc_coord, bass, 1, ^lootdrop_duration);
} else if ($dropint < 120) {
    obj_add(npc_coord, shark, 1, ^lootdrop_duration);
} else if ($dropint < 123) {
    obj_add(npc_coord, adamantite_ore, 1, ^lootdrop_duration);
} else if ($dropint < 128) {
    obj_add(npc_coord, ~ultrarare_getitem, ^lootdrop_duration);
}
