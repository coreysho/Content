// Fiyr Shadow - https://oldschool.runescape.wiki/w/Fiyr_Shadow

[ai_queue3,shadeshadow_level5] @shadeshadow_level5_drop_table;

[label,shadeshadow_level5_drop_table]
gosub(npc_death);
if (npc_findhero = ^false) {
    return;
}

obj_add(npc_coord, shade_bones5, 1, ^lootdrop_duration);

