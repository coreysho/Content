// Myre Blamish Snail - https://oldschool.runescape.wiki/w/Myre_Blamish_Snail

[ai_queue3,mmsnailpoint_swamp] @mmsnailpoint_swamp_drop_table;
[ai_queue3,mmsnailround_swamp] @mmsnailpoint_swamp_drop_table;

[label,mmsnailpoint_swamp_drop_table]
gosub(npc_death);
if (npc_findhero = ^false) {
    return;
}

obj_add(npc_coord, shellpoint_swamp, 1, ^lootdrop_duration);
obj_add(npc_coord, snail_corpse1, 1, ^lootdrop_duration);

