// Phrin Shadow - https://oldschool.runescape.wiki/w/Phrin_Shadow

[ai_queue3,shadeshadow_level2] @shadeshadow_level2_drop_table;

[label,shadeshadow_level2_drop_table]
gosub(npc_death);
if (npc_findhero = ^false) {
    return;
}

obj_add(npc_coord, shade_bones2, 1, ^lootdrop_duration);

