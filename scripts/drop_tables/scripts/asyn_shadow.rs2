// Asyn Shadow - https://oldschool.runescape.wiki/w/Asyn_Shadow

[ai_queue3,shadeshadow_level4] @shadeshadow_level4_drop_table;

[label,shadeshadow_level4_drop_table]
gosub(npc_death);
if (npc_findhero = ^false) {
    return;
}

obj_add(npc_coord, shade_bones4, 1, ^lootdrop_duration);

