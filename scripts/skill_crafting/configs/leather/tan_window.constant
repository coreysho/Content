// See smelt_window.constant: 100 draws the hide at its own icon scale. The tanner's cells are
// 96x128 with the model at the top, so a hide has more room here than a bar does.
^tan_window_scale = 100
