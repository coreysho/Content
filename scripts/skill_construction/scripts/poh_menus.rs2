// The two Construction windows, and the code that fills them.
//
// GENERATED by tools/genmenus.py from the same table that writes the .if files - the row
// blocks below are identical except for which component they name, and hand-keeping six of
// those in step with six more in the interface is how the first build panel went wrong.
// Edit the generator, not this file.
//
// WHY EVERY ROW IS A LAYER. Client.drawInterface tests a component's hide flag only when
// that component is the LAYER it is about to draw - a hidden text or model is drawn anyway.
// So an empty row is hidden by hiding its layer, and the row's contents come along.
//
// WHY THE ROW OUTLINE IS THE BUTTON. handleInterfaceInput hit-tests buttonType 1 without
// looking at the component type, so a type=rect can be the click target and cover the whole
// row, as OSRS's does. r0box..r5box and s0box..s7box are those rects.
//
// WHY if_openmain CAN SUSPEND. if_addresumebutton only pushes a component id onto the
// player's resumeButtons list, and IfButtonHandler resumes a PAUSEBUTTON script for any
// visible component on that list - isComponentVisible accepts modalMain exactly as it
// accepts modalChat. The chat menus are not a special case; they are just the only ones
// that had been written.
//
// WHY THE ZOOMS ARE A TABLE. The zoom is a DIVISOR - bigger means smaller - and a loc model
// hangs upward from the middle of its component because its origin is the floor. One shared
// value cannot frame 78 models of different sizes, so tools/ifmodels.py solves each one from
// its own geometry and writes the tables below.
//
// WHY THE TINTS ARE THREE ENUMS AND NOT A PROC. Colour comes from @xxx@ tags inside the text -
// PixFont.drawStringTag reads them mid-string, so one if_settext controls the colour of every run
// in a row without extra components. The tag itself has to come from somewhere, and a proc that
// returns a string has no precedent in this repo (claude/rs2-compile-traps.md), so each row works
// out an int state and looks the tag up: <enum(int, string, poh_tint_name, $state)>, which is the
// idiom poh_test.rs2 already compiles. State 0 is buildable, 1 is buildable but you cannot pay for
// it, 2 is above your Construction level.
//
// ON THE MODELS ARRIVING LATE. if_setmodel names a model the .if never mentions, so the packer
// does not flag it for the client's preload batch. Model.tryGet asks the on-demand stream for
// anything missing and returns null until it lands, so an icon can be blank for the first frame
// or two the very first time a window shows it. The furniture models are all flagged anyway -
// ~poh_furn_show spawns those same locs - and a room icon that blinks once per session is not
// worth a second table.


// =========================================================================== rooms

// The model each room is shown by, and the zoom that frames it. Raw model.pack ids:
// if_setmodel takes an id, not a symbol. Battery check 31 reads every one back out of
// model.pack and fails if a name ever moves under them.
[proc,poh_room_model](int $type)(int)
switch_int ($type) {
    case 1 : return(13184);   // loc_13478_8
    case 2 : return(12443);   // loc_13581_8
    case 3 : return(12625);   // poh_stove1_8
    case 4 : return(12408);   // poh_diningchairs1_8
    case 5 : return(12550);   // poh_bed1_8
    case 6 : return(10557);   // loc_10673i2_8
    case 7 : return(12377);   // poh_altar_saradomin1_8
    case 8 : return(12485);   // poh_globe1_8
    case 9 : return(12595);   // poh_throne1_8
    case 10 : return(11231);   // draynor_killerwatt_portal_8
    case 11 : return(12896);   // poh_archery_target1_8
    case 12 : return(12975);   // poh_hangman_chest_8
    case 13 : return(12462);   // poh_display_case_rune1_6_8
    case 14 : return(12415);   // loc_13597_8
    case 15 : return(11129);   // fever_repair_locker_8
    case default : return(13184);
}

[proc,poh_room_zoom](int $type)(int)
switch_int ($type) {
    case 1 : return(5096);
    case 2 : return(2824);
    case 3 : return(2104);
    case 4 : return(2847);
    case 5 : return(3794);
    case 6 : return(4506);
    case 7 : return(4074);
    case 8 : return(5195);
    case 9 : return(4274);
    case 10 : return(5082);
    case 11 : return(2418);
    case 12 : return(3361);
    case 13 : return(2916);
    case 14 : return(4457);
    case 15 : return(2364);
    case default : return(5096);
}

[proc,poh_room_row0](int $type)
if ($type = 0) {
    if_sethide(poh_roommenu:row0, true);
    return;
}
if_sethide(poh_roommenu:row0, false);
def_int $state = ^poh_state_ready;
if (inv_total(inv, coins) < enum(int, int, poh_room_cost, $type)) {
    $state = ^poh_state_poor;
}
if (stat(construction) < enum(int, int, poh_room_level, $type)) {
    $state = ^poh_state_locked;
}
if_setmodel(poh_roommenu:r0model, ~poh_room_model($type));
if_setangle(poh_roommenu:r0model, ^poh_menu_xan, ^poh_menu_yan, ~poh_room_zoom($type));
if_settext(poh_roommenu:r0name, "<enum(int, string, poh_tint_name, $state)><enum(int, string, poh_room_name, $type)>: <enum(int, string, poh_tint_level, $state)>Lvl <tostring(enum(int, int, poh_room_level, $type))>");
if_settext(poh_roommenu:r0cost, "<enum(int, string, poh_tint_need, $state)><enum(int, string, poh_room_cost_text, $type)> coins");

[proc,poh_room_row1](int $type)
if ($type = 0) {
    if_sethide(poh_roommenu:row1, true);
    return;
}
if_sethide(poh_roommenu:row1, false);
def_int $state = ^poh_state_ready;
if (inv_total(inv, coins) < enum(int, int, poh_room_cost, $type)) {
    $state = ^poh_state_poor;
}
if (stat(construction) < enum(int, int, poh_room_level, $type)) {
    $state = ^poh_state_locked;
}
if_setmodel(poh_roommenu:r1model, ~poh_room_model($type));
if_setangle(poh_roommenu:r1model, ^poh_menu_xan, ^poh_menu_yan, ~poh_room_zoom($type));
if_settext(poh_roommenu:r1name, "<enum(int, string, poh_tint_name, $state)><enum(int, string, poh_room_name, $type)>: <enum(int, string, poh_tint_level, $state)>Lvl <tostring(enum(int, int, poh_room_level, $type))>");
if_settext(poh_roommenu:r1cost, "<enum(int, string, poh_tint_need, $state)><enum(int, string, poh_room_cost_text, $type)> coins");

[proc,poh_room_row2](int $type)
if ($type = 0) {
    if_sethide(poh_roommenu:row2, true);
    return;
}
if_sethide(poh_roommenu:row2, false);
def_int $state = ^poh_state_ready;
if (inv_total(inv, coins) < enum(int, int, poh_room_cost, $type)) {
    $state = ^poh_state_poor;
}
if (stat(construction) < enum(int, int, poh_room_level, $type)) {
    $state = ^poh_state_locked;
}
if_setmodel(poh_roommenu:r2model, ~poh_room_model($type));
if_setangle(poh_roommenu:r2model, ^poh_menu_xan, ^poh_menu_yan, ~poh_room_zoom($type));
if_settext(poh_roommenu:r2name, "<enum(int, string, poh_tint_name, $state)><enum(int, string, poh_room_name, $type)>: <enum(int, string, poh_tint_level, $state)>Lvl <tostring(enum(int, int, poh_room_level, $type))>");
if_settext(poh_roommenu:r2cost, "<enum(int, string, poh_tint_need, $state)><enum(int, string, poh_room_cost_text, $type)> coins");

[proc,poh_room_row3](int $type)
if ($type = 0) {
    if_sethide(poh_roommenu:row3, true);
    return;
}
if_sethide(poh_roommenu:row3, false);
def_int $state = ^poh_state_ready;
if (inv_total(inv, coins) < enum(int, int, poh_room_cost, $type)) {
    $state = ^poh_state_poor;
}
if (stat(construction) < enum(int, int, poh_room_level, $type)) {
    $state = ^poh_state_locked;
}
if_setmodel(poh_roommenu:r3model, ~poh_room_model($type));
if_setangle(poh_roommenu:r3model, ^poh_menu_xan, ^poh_menu_yan, ~poh_room_zoom($type));
if_settext(poh_roommenu:r3name, "<enum(int, string, poh_tint_name, $state)><enum(int, string, poh_room_name, $type)>: <enum(int, string, poh_tint_level, $state)>Lvl <tostring(enum(int, int, poh_room_level, $type))>");
if_settext(poh_roommenu:r3cost, "<enum(int, string, poh_tint_need, $state)><enum(int, string, poh_room_cost_text, $type)> coins");

[proc,poh_room_row4](int $type)
if ($type = 0) {
    if_sethide(poh_roommenu:row4, true);
    return;
}
if_sethide(poh_roommenu:row4, false);
def_int $state = ^poh_state_ready;
if (inv_total(inv, coins) < enum(int, int, poh_room_cost, $type)) {
    $state = ^poh_state_poor;
}
if (stat(construction) < enum(int, int, poh_room_level, $type)) {
    $state = ^poh_state_locked;
}
if_setmodel(poh_roommenu:r4model, ~poh_room_model($type));
if_setangle(poh_roommenu:r4model, ^poh_menu_xan, ^poh_menu_yan, ~poh_room_zoom($type));
if_settext(poh_roommenu:r4name, "<enum(int, string, poh_tint_name, $state)><enum(int, string, poh_room_name, $type)>: <enum(int, string, poh_tint_level, $state)>Lvl <tostring(enum(int, int, poh_room_level, $type))>");
if_settext(poh_roommenu:r4cost, "<enum(int, string, poh_tint_need, $state)><enum(int, string, poh_room_cost_text, $type)> coins");

[proc,poh_room_row5](int $type)
if ($type = 0) {
    if_sethide(poh_roommenu:row5, true);
    return;
}
if_sethide(poh_roommenu:row5, false);
def_int $state = ^poh_state_ready;
if (inv_total(inv, coins) < enum(int, int, poh_room_cost, $type)) {
    $state = ^poh_state_poor;
}
if (stat(construction) < enum(int, int, poh_room_level, $type)) {
    $state = ^poh_state_locked;
}
if_setmodel(poh_roommenu:r5model, ~poh_room_model($type));
if_setangle(poh_roommenu:r5model, ^poh_menu_xan, ^poh_menu_yan, ~poh_room_zoom($type));
if_settext(poh_roommenu:r5name, "<enum(int, string, poh_tint_name, $state)><enum(int, string, poh_room_name, $type)>: <enum(int, string, poh_tint_level, $state)>Lvl <tostring(enum(int, int, poh_room_level, $type))>");
if_settext(poh_roommenu:r5cost, "<enum(int, string, poh_tint_need, $state)><enum(int, string, poh_room_cost_text, $type)> coins");

// Six rooms a page, three pages, which is every room type twice over. The page loop is
// bounded rather than while(true): a menu that cannot end would hang the player script.
//
// A click on a room you have the level for returns it and the caller does the paying. A click
// on one you do not says so and leaves the window open - money is a thing you go and fix, a
// level is a thing you go on browsing past.
[proc,poh_pick_room](int $rx, int $rz, int $side)(int)
def_int $page = 0;
while ($page < 3) {
    def_int $a = ~poh_nth_room($rx, $rz, $side, calc($page * ^poh_menu_rows + 0));
    def_int $b = ~poh_nth_room($rx, $rz, $side, calc($page * ^poh_menu_rows + 1));
    def_int $c = ~poh_nth_room($rx, $rz, $side, calc($page * ^poh_menu_rows + 2));
    def_int $d = ~poh_nth_room($rx, $rz, $side, calc($page * ^poh_menu_rows + 3));
    def_int $e = ~poh_nth_room($rx, $rz, $side, calc($page * ^poh_menu_rows + 4));
    def_int $f = ~poh_nth_room($rx, $rz, $side, calc($page * ^poh_menu_rows + 5));
    def_int $more = ~poh_nth_room($rx, $rz, $side, calc($page * ^poh_menu_rows + ^poh_menu_rows));
    if ($a = 0) {
        return(0);
    }
    if_settext(poh_roommenu:subtitle, "Select a room to build");
    ~poh_room_row0($a);
    ~poh_room_row1($b);
    ~poh_room_row2($c);
    ~poh_room_row3($d);
    ~poh_room_row4($e);
    ~poh_room_row5($f);
    if ($more = 0) {
        if_sethide(poh_roommenu:more, true);
    } else {
        if_sethide(poh_roommenu:more, false);
    }
    if_openmain(poh_roommenu);
    if ($a ! 0) {
        if_addresumebutton(poh_roommenu:r0box);
    }
    if ($b ! 0) {
        if_addresumebutton(poh_roommenu:r1box);
    }
    if ($c ! 0) {
        if_addresumebutton(poh_roommenu:r2box);
    }
    if ($d ! 0) {
        if_addresumebutton(poh_roommenu:r3box);
    }
    if ($e ! 0) {
        if_addresumebutton(poh_roommenu:r4box);
    }
    if ($f ! 0) {
        if_addresumebutton(poh_roommenu:r5box);
    }
    if ($more ! 0) {
        if_addresumebutton(poh_roommenu:morebtn);
    }
    if_addresumebutton(poh_roommenu:cancel);
    p_pausebutton;
    def_int $pick = 0;
    switch_component (last_com) {
        case poh_roommenu:r0box : $pick = $a;
        case poh_roommenu:r1box : $pick = $b;
        case poh_roommenu:r2box : $pick = $c;
        case poh_roommenu:r3box : $pick = $d;
        case poh_roommenu:r4box : $pick = $e;
        case poh_roommenu:r5box : $pick = $f;
        case poh_roommenu:morebtn : $page = calc($page + 1);
        case default : if_close; return(0);
    }
    if ($pick > 0) {
        if (stat(construction) >= enum(int, int, poh_room_level, $pick)) {
            if_close;
            return($pick);
        }
        mes("You need a Construction level of <tostring(enum(int, int, poh_room_level, $pick))> to build a <enum(int, string, poh_room_name, $pick)>.");
    }
}
if_close;
return(0);

// =========================================================================== furniture

// A framed picture has no depth: seen three-quarters on it is a one-pixel sliver, so the
// families that are pictures get turned face-on. Everything else keeps the shared camera.
// tools/furnspec.json holds the list and says why each one is on it.
[proc,poh_furn_xan](int $fam)(int)
switch_int ($fam) {
    case 32 : return(120);   // wallchart
    case 36 : return(120);   // thronedecor
    case 46 : return(120);   // portrait
    case 47 : return(120);   // landscape
    case 48 : return(120);   // guildtrophy
    case 49 : return(120);   // sword
    case 50 : return(120);   // map
    case default : return(^poh_menu_xan);
}

[proc,poh_furn_yan](int $fam)(int)
switch_int ($fam) {
    case 32 : return(1536);   // wallchart
    case 36 : return(1536);   // thronedecor
    case 46 : return(1536);   // portrait
    case 47 : return(1536);   // landscape
    case 48 : return(1536);   // guildtrophy
    case 49 : return(1536);   // sword
    case 50 : return(1536);   // map
    case default : return(^poh_menu_yan);
}

[proc,poh_furn_zoom](int $item)(int)
switch_int ($item) {
    case 1 : return(1883);   // poh_stove1_8
    case 2 : return(2288);   // poh_stove2_8
    case 3 : return(2288);   // poh_stove2_8
    case 4 : return(2952);   // poh_stove4_8
    case 5 : return(4000);   // poh_stove5_8
    case 6 : return(3131);   // 100_dave_stove_8
    case 7 : return(3260);   // poh_stove_7_8
    case 8 : return(3325);   // poh_sink1_8
    case 9 : return(2863);   // poh_sink2_8
    case 10 : return(3324);   // poh_sink3_8
    case 11 : return(4904);   // poh_larder1_8
    case 12 : return(4904);   // poh_larder2_8
    case 13 : return(4943);   // poh_larder3_8
    case 14 : return(3662);   // poh_kitchen_shelves1_q
    case 15 : return(3943);   // 100_dave_cook_shelf_q
    case 16 : return(3788);   // poh_kitchen_shelves3_q
    case 17 : return(3943);   // poh_kitchen_shelves4_q
    case 18 : return(3788);   // poh_kitchen_shelves5_q
    case 19 : return(3788);   // poh_kitchen_shelves_6_q
    case 20 : return(3788);   // poh_kitchen_shelves_7_q
    case 21 : return(4260);   // poh_kitchentable1_8
    case 22 : return(3898);   // poh_kitchentable2_8
    case 23 : return(4333);   // poh_kitchentable3_8
    case 24 : return(4085);   // poh_barrel1_8
    case 25 : return(4085);   // poh_barrel2_8
    case 26 : return(4085);   // poh_barrel3_8
    case 27 : return(4085);   // poh_barrel4_8
    case 28 : return(4085);   // poh_barrel5_8
    case 29 : return(4085);   // poh_barrel_6_8
    case 30 : return(7965);   // poh_diningtable1_8
    case 31 : return(7398);   // poh_diningtable2_8
    case 32 : return(7398);   // poh_diningtable3_8
    case 33 : return(8039);   // poh_diningtable4_8
    case 34 : return(8039);   // poh_diningtable5_8
    case 35 : return(7483);   // poh_diningtable_6_8
    case 36 : return(7483);   // poh_diningtable_7_8
    case 37 : return(2554);   // poh_diningchairs1_8
    case 38 : return(2874);   // poh_diningchairs2_8
    case 39 : return(2874);   // poh_diningchairs3_8
    case 40 : return(2721);   // poh_diningchairs4_8
    case 41 : return(2721);   // poh_diningchairs5_8
    case 42 : return(2808);   // poh_diningchairs_6_8
    case 43 : return(2808);   // poh_diningchairs_7_8
    case 44 : return(4398);   // poh_bellpull1_8
    case 45 : return(4398);   // poh_bellpull2_8
    case 46 : return(4398);   // poh_bellpull3_8
    case 47 : return(3325);   // poh_bed1_8
    case 48 : return(3767);   // poh_bed2_8
    case 49 : return(4061);   // poh_bed3_8
    case 50 : return(3707);   // poh_bed4_8
    case 51 : return(4305);   // poh_bed5_8
    case 52 : return(5657);   // poh_bed_6_8
    case 53 : return(5657);   // poh_bed_7_8
    case 54 : return(2123);   // fever_repair_locker_8
    case 55 : return(2824);   // poh_wardrobe2_8
    case 56 : return(4144);   // vm_digsite_finds_talisman_8
    case 57 : return(2828);   // poh_wardrobe4_8
    case 58 : return(4144);   // poh_wardrobe5_8
    case 59 : return(4333);   // poh_wardrobe_6_8
    case 60 : return(4333);   // poh_wardrobe_7_8
    case 61 : return(3873);   // poh_clock1_8
    case 62 : return(4204);   // 100_dave_grandfatherclock_8
    case 63 : return(4204);   // poh_clock3_8
    case 64 : return(2536);   // loc_13581_8
    case 65 : return(2763);   // loc_13582_8
    case 66 : return(2763);   // qip_sheep_shearer_rocking_chair_8
    case 67 : return(2991);   // loc_13584_8
    case 68 : return(2991);   // loc_13585_8
    case 69 : return(2644);   // loc_13586_8
    case 70 : return(3067);   // loc_13587_8
    case 71 : return(4000);   // loc_13597_8
    case 72 : return(4322);   // loc_13598_8
    case 73 : return(4391);   // loc_13599_8
    case 74 : return(3577);   // loc_13600_8
    case 75 : return(3882);   // loc_13601_8
    case 76 : return(3921);   // loc_13602_8
    case 77 : return(2773);   // loc_13609_8
    case 78 : return(3793);   // loc_13611_8
    case 79 : return(5404);   // loc_13613_8
    case 80 : return(2815);   // poh_mirror1_8
    case 81 : return(3277);   // poh_mirror2_8
    case 82 : return(3485);   // poh_mirror3_8
    case 83 : return(4039);   // poh_mirror4_8
    case 84 : return(4039);   // poh_mirror5_8
    case 85 : return(3356);   // poh_mirror_6_8
    case 86 : return(3300);   // poh_mirror_7_8
    case 87 : return(2011);   // poh_pet1_0
    case 88 : return(2041);   // poh_pet2_0
    case 89 : return(2041);   // poh_pet3_0
    case 90 : return(4041);   // loc_10673i2_8
    case 91 : return(3389);   // loc_10676_8
    case 92 : return(3448);   // loc_13706_8
    case 93 : return(3448);   // loc_13706_8
    case 94 : return(3448);   // loc_13706_8
    case 95 : return(4504);   // loc_13699_q
    case 96 : return(4504);   // loc_13700_q
    case 97 : return(4504);   // loc_13701_q
    case 98 : return(4504);   // loc_13702_q
    case 99 : return(4589);   // loc_13703_q
    case 100 : return(3300);   // loc_13709_8
    case 101 : return(3300);   // loc_13709_8
    case 102 : return(3300);   // loc_13709_8
    case 103 : return(3300);   // loc_13712_8
    case 104 : return(4041);   // loc_13713_8
    case 105 : return(3128);   // devious_whetstone_8
    case 106 : return(3085);   // loc_13715_8
    case 107 : return(3085);   // loc_13716_8
    case 108 : return(2891);   // loc_13717_8
    case 109 : return(3379);   // loc_13718_8
    case 110 : return(3655);   // poh_altar_saradomin1_8
    case 111 : return(3656);   // poh_altar_saradomin2_8
    case 112 : return(3958);   // poh_altar_saradomin3_8
    case 113 : return(3722);   // poh_altar_saradomin4_8
    case 114 : return(3656);   // poh_altar_saradomin5_8
    case 115 : return(3656);   // poh_altar_saradomin_6_8
    case 116 : return(3656);   // poh_altar_saradomin_7_8
    case 117 : return(3734);   // poh_icon1_8
    case 118 : return(3734);   // poh_icon2_8
    case 119 : return(3734);   // poh_icon3_8
    case 120 : return(3398);   // poh_icon4_8
    case 121 : return(4265);   // poh_icon5_8
    case 122 : return(3801);   // poh_icon_6_8
    case 123 : return(3623);   // poh_icon_7_8
    case 124 : return(2250);   // poh_torch1_8
    case 125 : return(2250);   // poh_torch2_8
    case 126 : return(2518);   // poh_torch3_8
    case 127 : return(2518);   // poh_torch4_8
    case 128 : return(3270);   // poh_torch5_8
    case 129 : return(3188);   // poh_torch_6_8
    case 130 : return(6822);   // poh_torch_7_8
    case 131 : return(1851);   // poh_statue_bob1_8
    case 132 : return(2259);   // poh_statue_bob2_8
    case 133 : return(2874);   // poh_statue_bob3_8
    case 134 : return(3835);   // poh_statue_angel_8
    case 135 : return(4452);   // poh_statue_delrith_8
    case 136 : return(3547);   // poh_statue_druid_8
    case 137 : return(3153);   // poh_statue_guthix_8
    case 138 : return(1807);   // poh_statue_juna_8
    case 139 : return(4191);   // poh_statue_lucien_8
    case 140 : return(3511);   // poh_statue_monk_8
    case 141 : return(5439);   // poh_statue_saradomin_8
    case 142 : return(4587);   // poh_statue_zamorak_8
    case 143 : return(4781);   // poh_musical_thing1_8
    case 144 : return(4749);   // poh_musical_thing2_8
    case 145 : return(4412);   // poh_musical_thing3_8
    case 146 : return(2685);   // poh_lectern1_8
    case 147 : return(2653);   // poh_lectern2_8
    case 148 : return(2646);   // poh_lectern3_8
    case 149 : return(2653);   // poh_lectern4_8
    case 150 : return(2653);   // poh_lectern5_8
    case 151 : return(2653);   // poh_lectern_6_8
    case 152 : return(2646);   // poh_lectern_7_8
    case 153 : return(4650);   // poh_globe1_8
    case 154 : return(5447);   // poh_globe2_8
    case 155 : return(5447);   // poh_globe3_8
    case 156 : return(5479);   // poh_globe4_8
    case 157 : return(5286);   // poh_globe5_8
    case 158 : return(5591);   // poh_globe_6_8
    case 159 : return(6861);   // poh_globe_7_8
    case 160 : return(3020);   // poh_crystalball1_8
    case 161 : return(3927);   // poh_crystalball2_8
    case 162 : return(3970);   // poh_crystalball3_8
    case 163 : return(3031);   // poh_telescope1_8
    case 164 : return(3031);   // poh_telescope2_8
    case 165 : return(3031);   // poh_telescope3_8
    case 166 : return(3424);   // poh_wallchart1_q
    case 167 : return(3596);   // poh_wallchart2_q
    case 168 : return(3868);   // 100_dave_wallchart_q
    case 169 : return(3857);   // poh_throne1_8
    case 170 : return(3693);   // poh_throne2_8
    case 171 : return(4288);   // poh_throne3_8
    case 172 : return(4288);   // poh_throne4_8
    case 173 : return(3302);   // poh_throne5_8
    case 174 : return(3500);   // poh_throne_6_8
    case 175 : return(4058);   // poh_throne_7_8
    case 176 : return(2721);   // poh_diningchairs5_8
    case 177 : return(2808);   // poh_diningchairs_6_8
    case 178 : return(2808);   // poh_diningchairs_7_8
    case 179 : return(3374);   // poh_lever_oak4_8
    case 180 : return(3374);   // poh_lever_teak4_8
    case 181 : return(3374);   // poh_lever_mag4_8
    case 182 : return(5060);   // poh_throne_backdecor_cloth_8
    case 183 : return(3129);   // poh_throne_backdecor_stainedglass_8
    case 184 : return(3680);   // poh_dartboard1_8
    case 185 : return(3680);   // poh_dartboard1_8
    case 186 : return(3007);   // poh_hangman_chest_8
    case 187 : return(2160);   // poh_mime_jester_8
    case 188 : return(1923);   // qip_sheep_shearer_chestclosed_8
    case 189 : return(1923);   // poh_treasure_teak_chest_8
    case 190 : return(1923);   // fever_chestclosed_8
    case 191 : return(2135);   // poh_tbt_limestone_new_8
    case 192 : return(2135);   // poh_tbt_limestone_new_8
    case 193 : return(2135);   // poh_tbt_limestone_new_8
    case 194 : return(1054);   // poh_elemental_orb1_8
    case 195 : return(1054);   // poh_elemental_orb1_8
    case 196 : return(1054);   // poh_elemental_orb1_8
    case 197 : return(1551);   // poh_magic_circle_mat_0
    case 198 : return(2169);   // poh_archery_target1_8
    case 199 : return(2169);   // poh_archery_target1_8
    case 200 : return(3491);   // poh_armour_mithril5_8
    case 201 : return(3491);   // poh_armour_mithril5_8
    case 202 : return(3491);   // poh_armour_mithril5_8
    case 203 : return(3330);   // poh_armour_castlewars_red_6_8
    case 204 : return(3330);   // poh_armour_castlewars_red_6_8
    case 205 : return(3330);   // poh_armour_castlewars_red_6_8
    case 206 : return(2626);   // poh_display_case_rune1_6_8
    case 207 : return(2626);   // poh_display_case_rune1_6_8
    case 208 : return(2626);   // poh_display_case_rune1_6_8
    case 209 : return(2522);   // loc_13481_8
    case 210 : return(3763);   // vm_digsite_finds_tablet_8
    case 211 : return(3828);   // vm_digsite_finds_coin_senntisten_8
    case 212 : return(3612);   // loc_12235_8
    case 213 : return(4144);   // loc_13485_8
    case 214 : return(4209);   // loc_13486_8
    case 215 : return(4162);   // loc_13487_8
    case 216 : return(3722);   // loc_12236_8
    case 217 : return(3680);   // loc_12237_8
    case 218 : return(3923);   // loc_13490_8
    case 219 : return(3530);   // poh_portrait_elena1_q
    case 220 : return(3627);   // poh_portrait_giantdwarf1_q
    case 221 : return(3515);   // poh_portrait_kingarthur1_q
    case 222 : return(3509);   // poh_landscape_desert1_q
    case 223 : return(3502);   // poh_landscape_istafar1_q
    case 224 : return(3509);   // poh_landscape_karamja1_q
    case 225 : return(3509);   // poh_landscape_lumbridge1_q
    case 226 : return(3509);   // poh_landscape_morytania1_q
    case 227 : return(3502);   // poh_trophy_amuletofglory4_q
    case 228 : return(3746);   // poh_trophy_antidragonbreath4_q
    case 229 : return(3628);   // poh_trophy_legendscape4_q
    case 230 : return(3165);   // poh_trophy_silverlight5_q
    case 231 : return(3165);   // poh_trophy_silverlight5_q
    case 232 : return(3165);   // poh_trophy_excalibur5_q
    case 233 : return(2779);   // poh_wall_map_freearea_q
    case 234 : return(2924);   // poh_wall_map_world_q
    case 235 : return(1923);   // qip_sheep_shearer_chestclosed_8
    case 236 : return(1923);   // poh_treasure_teak_chest_8
    case 237 : return(1923);   // fever_chestclosed_8
    case 238 : return(1923);   // poh_treasure_magic_chest_8
    case default : return(1883);
}

[proc,poh_furn_slot0](int $item)
if ($item = 0) {
    if_sethide(poh_furnmenu:slot0, true);
    return;
}
if_sethide(poh_furnmenu:slot0, false);
def_int $state = ^poh_state_ready;
if (~poh_furn_plank_total(enum(int, int, poh_furn_wood, $item)) < enum(int, int, poh_furn_planks, $item)) {
    $state = ^poh_state_poor;
}
if (stat(construction) < enum(int, int, poh_furn_level, $item)) {
    $state = ^poh_state_locked;
}
def_int $fam = enum(int, int, poh_furn_fam, $item);
if_setmodel(poh_furnmenu:s0model, ~poh_furn_model($item));
if_setangle(poh_furnmenu:s0model, ~poh_furn_xan($fam), ~poh_furn_yan($fam), ~poh_furn_zoom($item));
if_settext(poh_furnmenu:s0lvl, "<enum(int, string, poh_tint_level, $state)>Level <tostring(enum(int, int, poh_furn_level, $item))>");
if_settext(poh_furnmenu:s0name, "<enum(int, string, poh_tint_name, $state)><enum(int, string, poh_furn_name, $item)>");
if_settext(poh_furnmenu:s0need, "<enum(int, string, poh_tint_need, $state)><tostring(enum(int, int, poh_furn_planks, $item))> <enum(int, string, poh_wood_name, $item)>");

[proc,poh_furn_slot1](int $item)
if ($item = 0) {
    if_sethide(poh_furnmenu:slot1, true);
    return;
}
if_sethide(poh_furnmenu:slot1, false);
def_int $state = ^poh_state_ready;
if (~poh_furn_plank_total(enum(int, int, poh_furn_wood, $item)) < enum(int, int, poh_furn_planks, $item)) {
    $state = ^poh_state_poor;
}
if (stat(construction) < enum(int, int, poh_furn_level, $item)) {
    $state = ^poh_state_locked;
}
def_int $fam = enum(int, int, poh_furn_fam, $item);
if_setmodel(poh_furnmenu:s1model, ~poh_furn_model($item));
if_setangle(poh_furnmenu:s1model, ~poh_furn_xan($fam), ~poh_furn_yan($fam), ~poh_furn_zoom($item));
if_settext(poh_furnmenu:s1lvl, "<enum(int, string, poh_tint_level, $state)>Level <tostring(enum(int, int, poh_furn_level, $item))>");
if_settext(poh_furnmenu:s1name, "<enum(int, string, poh_tint_name, $state)><enum(int, string, poh_furn_name, $item)>");
if_settext(poh_furnmenu:s1need, "<enum(int, string, poh_tint_need, $state)><tostring(enum(int, int, poh_furn_planks, $item))> <enum(int, string, poh_wood_name, $item)>");

[proc,poh_furn_slot2](int $item)
if ($item = 0) {
    if_sethide(poh_furnmenu:slot2, true);
    return;
}
if_sethide(poh_furnmenu:slot2, false);
def_int $state = ^poh_state_ready;
if (~poh_furn_plank_total(enum(int, int, poh_furn_wood, $item)) < enum(int, int, poh_furn_planks, $item)) {
    $state = ^poh_state_poor;
}
if (stat(construction) < enum(int, int, poh_furn_level, $item)) {
    $state = ^poh_state_locked;
}
def_int $fam = enum(int, int, poh_furn_fam, $item);
if_setmodel(poh_furnmenu:s2model, ~poh_furn_model($item));
if_setangle(poh_furnmenu:s2model, ~poh_furn_xan($fam), ~poh_furn_yan($fam), ~poh_furn_zoom($item));
if_settext(poh_furnmenu:s2lvl, "<enum(int, string, poh_tint_level, $state)>Level <tostring(enum(int, int, poh_furn_level, $item))>");
if_settext(poh_furnmenu:s2name, "<enum(int, string, poh_tint_name, $state)><enum(int, string, poh_furn_name, $item)>");
if_settext(poh_furnmenu:s2need, "<enum(int, string, poh_tint_need, $state)><tostring(enum(int, int, poh_furn_planks, $item))> <enum(int, string, poh_wood_name, $item)>");

[proc,poh_furn_slot3](int $item)
if ($item = 0) {
    if_sethide(poh_furnmenu:slot3, true);
    return;
}
if_sethide(poh_furnmenu:slot3, false);
def_int $state = ^poh_state_ready;
if (~poh_furn_plank_total(enum(int, int, poh_furn_wood, $item)) < enum(int, int, poh_furn_planks, $item)) {
    $state = ^poh_state_poor;
}
if (stat(construction) < enum(int, int, poh_furn_level, $item)) {
    $state = ^poh_state_locked;
}
def_int $fam = enum(int, int, poh_furn_fam, $item);
if_setmodel(poh_furnmenu:s3model, ~poh_furn_model($item));
if_setangle(poh_furnmenu:s3model, ~poh_furn_xan($fam), ~poh_furn_yan($fam), ~poh_furn_zoom($item));
if_settext(poh_furnmenu:s3lvl, "<enum(int, string, poh_tint_level, $state)>Level <tostring(enum(int, int, poh_furn_level, $item))>");
if_settext(poh_furnmenu:s3name, "<enum(int, string, poh_tint_name, $state)><enum(int, string, poh_furn_name, $item)>");
if_settext(poh_furnmenu:s3need, "<enum(int, string, poh_tint_need, $state)><tostring(enum(int, int, poh_furn_planks, $item))> <enum(int, string, poh_wood_name, $item)>");

[proc,poh_furn_slot4](int $item)
if ($item = 0) {
    if_sethide(poh_furnmenu:slot4, true);
    return;
}
if_sethide(poh_furnmenu:slot4, false);
def_int $state = ^poh_state_ready;
if (~poh_furn_plank_total(enum(int, int, poh_furn_wood, $item)) < enum(int, int, poh_furn_planks, $item)) {
    $state = ^poh_state_poor;
}
if (stat(construction) < enum(int, int, poh_furn_level, $item)) {
    $state = ^poh_state_locked;
}
def_int $fam = enum(int, int, poh_furn_fam, $item);
if_setmodel(poh_furnmenu:s4model, ~poh_furn_model($item));
if_setangle(poh_furnmenu:s4model, ~poh_furn_xan($fam), ~poh_furn_yan($fam), ~poh_furn_zoom($item));
if_settext(poh_furnmenu:s4lvl, "<enum(int, string, poh_tint_level, $state)>Level <tostring(enum(int, int, poh_furn_level, $item))>");
if_settext(poh_furnmenu:s4name, "<enum(int, string, poh_tint_name, $state)><enum(int, string, poh_furn_name, $item)>");
if_settext(poh_furnmenu:s4need, "<enum(int, string, poh_tint_need, $state)><tostring(enum(int, int, poh_furn_planks, $item))> <enum(int, string, poh_wood_name, $item)>");

[proc,poh_furn_slot5](int $item)
if ($item = 0) {
    if_sethide(poh_furnmenu:slot5, true);
    return;
}
if_sethide(poh_furnmenu:slot5, false);
def_int $state = ^poh_state_ready;
if (~poh_furn_plank_total(enum(int, int, poh_furn_wood, $item)) < enum(int, int, poh_furn_planks, $item)) {
    $state = ^poh_state_poor;
}
if (stat(construction) < enum(int, int, poh_furn_level, $item)) {
    $state = ^poh_state_locked;
}
def_int $fam = enum(int, int, poh_furn_fam, $item);
if_setmodel(poh_furnmenu:s5model, ~poh_furn_model($item));
if_setangle(poh_furnmenu:s5model, ~poh_furn_xan($fam), ~poh_furn_yan($fam), ~poh_furn_zoom($item));
if_settext(poh_furnmenu:s5lvl, "<enum(int, string, poh_tint_level, $state)>Level <tostring(enum(int, int, poh_furn_level, $item))>");
if_settext(poh_furnmenu:s5name, "<enum(int, string, poh_tint_name, $state)><enum(int, string, poh_furn_name, $item)>");
if_settext(poh_furnmenu:s5need, "<enum(int, string, poh_tint_need, $state)><tostring(enum(int, int, poh_furn_planks, $item))> <enum(int, string, poh_wood_name, $item)>");

[proc,poh_furn_slot6](int $item)
if ($item = 0) {
    if_sethide(poh_furnmenu:slot6, true);
    return;
}
if_sethide(poh_furnmenu:slot6, false);
def_int $state = ^poh_state_ready;
if (~poh_furn_plank_total(enum(int, int, poh_furn_wood, $item)) < enum(int, int, poh_furn_planks, $item)) {
    $state = ^poh_state_poor;
}
if (stat(construction) < enum(int, int, poh_furn_level, $item)) {
    $state = ^poh_state_locked;
}
def_int $fam = enum(int, int, poh_furn_fam, $item);
if_setmodel(poh_furnmenu:s6model, ~poh_furn_model($item));
if_setangle(poh_furnmenu:s6model, ~poh_furn_xan($fam), ~poh_furn_yan($fam), ~poh_furn_zoom($item));
if_settext(poh_furnmenu:s6lvl, "<enum(int, string, poh_tint_level, $state)>Level <tostring(enum(int, int, poh_furn_level, $item))>");
if_settext(poh_furnmenu:s6name, "<enum(int, string, poh_tint_name, $state)><enum(int, string, poh_furn_name, $item)>");
if_settext(poh_furnmenu:s6need, "<enum(int, string, poh_tint_need, $state)><tostring(enum(int, int, poh_furn_planks, $item))> <enum(int, string, poh_wood_name, $item)>");

[proc,poh_furn_slot7](int $item)
if ($item = 0) {
    if_sethide(poh_furnmenu:slot7, true);
    return;
}
if_sethide(poh_furnmenu:slot7, false);
def_int $state = ^poh_state_ready;
if (~poh_furn_plank_total(enum(int, int, poh_furn_wood, $item)) < enum(int, int, poh_furn_planks, $item)) {
    $state = ^poh_state_poor;
}
if (stat(construction) < enum(int, int, poh_furn_level, $item)) {
    $state = ^poh_state_locked;
}
def_int $fam = enum(int, int, poh_furn_fam, $item);
if_setmodel(poh_furnmenu:s7model, ~poh_furn_model($item));
if_setangle(poh_furnmenu:s7model, ~poh_furn_xan($fam), ~poh_furn_yan($fam), ~poh_furn_zoom($item));
if_settext(poh_furnmenu:s7lvl, "<enum(int, string, poh_tint_level, $state)>Level <tostring(enum(int, int, poh_furn_level, $item))>");
if_settext(poh_furnmenu:s7name, "<enum(int, string, poh_tint_name, $state)><enum(int, string, poh_furn_name, $item)>");
if_settext(poh_furnmenu:s7need, "<enum(int, string, poh_tint_need, $state)><tostring(enum(int, int, poh_furn_planks, $item))> <enum(int, string, poh_wood_name, $item)>");

// Eight slots is one more than the largest family has tiers, so the More button is dead
// weight today and there anyway: the tables are data, and a family can grow.
//
// Level is not a filter - the whole family is listed and the tiers above you are dimmed, so
// the window is also where you find out what the next twenty levels are for.
[proc,poh_furn_pick](int $fam)(int)
def_int $page = 0;
while ($page < 3) {
    def_int $a = ~poh_furn_nth($fam, calc($page * ^poh_menu_slots + 0));
    def_int $b = ~poh_furn_nth($fam, calc($page * ^poh_menu_slots + 1));
    def_int $c = ~poh_furn_nth($fam, calc($page * ^poh_menu_slots + 2));
    def_int $d = ~poh_furn_nth($fam, calc($page * ^poh_menu_slots + 3));
    def_int $e = ~poh_furn_nth($fam, calc($page * ^poh_menu_slots + 4));
    def_int $f = ~poh_furn_nth($fam, calc($page * ^poh_menu_slots + 5));
    def_int $g = ~poh_furn_nth($fam, calc($page * ^poh_menu_slots + 6));
    def_int $h = ~poh_furn_nth($fam, calc($page * ^poh_menu_slots + 7));
    def_int $more = ~poh_furn_nth($fam, calc($page * ^poh_menu_slots + ^poh_menu_slots));
    if ($a = 0) {
        return(0);
    }
    if_settext(poh_furnmenu:title, "<enum(int, string, poh_fam_name, $fam)>");
    if_settext(poh_furnmenu:subtitle, "Select what you want to build");
    ~poh_furn_slot0($a);
    ~poh_furn_slot1($b);
    ~poh_furn_slot2($c);
    ~poh_furn_slot3($d);
    ~poh_furn_slot4($e);
    ~poh_furn_slot5($f);
    ~poh_furn_slot6($g);
    ~poh_furn_slot7($h);
    if ($more = 0) {
        if_sethide(poh_furnmenu:more, true);
    } else {
        if_sethide(poh_furnmenu:more, false);
    }
    if_openmain(poh_furnmenu);
    if ($a ! 0) {
        if_addresumebutton(poh_furnmenu:s0box);
    }
    if ($b ! 0) {
        if_addresumebutton(poh_furnmenu:s1box);
    }
    if ($c ! 0) {
        if_addresumebutton(poh_furnmenu:s2box);
    }
    if ($d ! 0) {
        if_addresumebutton(poh_furnmenu:s3box);
    }
    if ($e ! 0) {
        if_addresumebutton(poh_furnmenu:s4box);
    }
    if ($f ! 0) {
        if_addresumebutton(poh_furnmenu:s5box);
    }
    if ($g ! 0) {
        if_addresumebutton(poh_furnmenu:s6box);
    }
    if ($h ! 0) {
        if_addresumebutton(poh_furnmenu:s7box);
    }
    if ($more ! 0) {
        if_addresumebutton(poh_furnmenu:morebtn);
    }
    if_addresumebutton(poh_furnmenu:cancel);
    p_pausebutton;
    def_int $pick = 0;
    switch_component (last_com) {
        case poh_furnmenu:s0box : $pick = $a;
        case poh_furnmenu:s1box : $pick = $b;
        case poh_furnmenu:s2box : $pick = $c;
        case poh_furnmenu:s3box : $pick = $d;
        case poh_furnmenu:s4box : $pick = $e;
        case poh_furnmenu:s5box : $pick = $f;
        case poh_furnmenu:s6box : $pick = $g;
        case poh_furnmenu:s7box : $pick = $h;
        case poh_furnmenu:morebtn : $page = calc($page + 1);
        case default : if_close; return(0);
    }
    if ($pick > 0) {
        if (stat(construction) >= enum(int, int, poh_furn_level, $pick)) {
            if_close;
            return($pick);
        }
        mes("You need a Construction level of <tostring(enum(int, int, poh_furn_level, $pick))> to build that.");
    }
}
if_close;
return(0);
