// Rugs: one piece of furniture over a whole rectangle of floor.
//
// GENERATED by tools/genrugs.py from the six template squares. Do not edit by hand.
//
// Five rooms have a Rug space and they are different sizes - 36 tiles in the Skill hall and
// the Quest hall, 24 in the Bedroom, 16 in the Parlour, 10 in the Chapel - so the room has to
// be looked up before anything is laid. Which piece goes on which tile is the rectangle's own
// geometry, and the hotspot ghosts agree exactly: the first tier's three pieces ARE the three
// ghost models, vertex for vertex, which is what names corner, side and middle rather than
// leaving it to a guess. The generator refuses to write if the two ever stop agreeing.
//
// The rotation is done at runtime by ~poh_ring_tile and ~poh_ring_angle, which are the engine's
// own zone maths (poh_combat_ring.rs2). Rugs and rings are the same problem and share it.

// =========================================================================== the pieces

[proc,poh_rug_loc](int $tier, int $kind)(loc)
switch_int (calc($tier * 4 + $kind)) {
    case 4 : return(loc_13588);   // brown corner
    case 5 : return(loc_13589);   // brown side
    case 6 : return(loc_13590);   // brown middle
    case 8 : return(loc_13591);   // plain corner
    case 9 : return(loc_13592);   // plain side
    case 10 : return(loc_13593);   // plain middle
    case 12 : return(loc_13594);   // opulent corner
    case 13 : return(loc_13595);   // opulent side
    case 14 : return(loc_13596);   // opulent middle
    case default : return(null);
}

[proc,poh_rug_lay](coord $base, int $rot, int $x, int $z, int $angle, int $tier, int $kind)
loc_add(~poh_ring_tile($base, $rot, $x, $z), ~poh_rug_loc($tier, $kind), ~poh_ring_angle($angle, $rot), grounddecor, ^poh_loc_duration);

// =========================================================================== the rooms

// Bedroom: 24 tiles
[proc,poh_rug_bedroom](coord $base, int $rot, int $tier)
~poh_rug_lay($base, $rot, 1, 1, 3, $tier, ^poh_rug_corner);
~poh_rug_lay($base, $rot, 1, 2, 3, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 1, 3, 3, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 1, 4, 0, $tier, ^poh_rug_corner);
~poh_rug_lay($base, $rot, 2, 1, 2, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 2, 2, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 2, 3, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 2, 4, 0, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 3, 1, 2, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 3, 2, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 3, 3, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 3, 4, 0, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 4, 1, 2, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 4, 2, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 4, 3, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 4, 4, 0, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 5, 1, 2, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 5, 2, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 5, 3, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 5, 4, 0, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 6, 1, 2, $tier, ^poh_rug_corner);
~poh_rug_lay($base, $rot, 6, 2, 1, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 6, 3, 1, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 6, 4, 1, $tier, ^poh_rug_corner);

// Chapel: 10 tiles
[proc,poh_rug_chapel](coord $base, int $rot, int $tier)
~poh_rug_lay($base, $rot, 3, 0, 3, $tier, ^poh_rug_corner);
~poh_rug_lay($base, $rot, 3, 1, 3, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 3, 2, 3, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 3, 3, 3, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 3, 4, 0, $tier, ^poh_rug_corner);
~poh_rug_lay($base, $rot, 4, 0, 2, $tier, ^poh_rug_corner);
~poh_rug_lay($base, $rot, 4, 1, 1, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 4, 2, 1, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 4, 3, 1, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 4, 4, 1, $tier, ^poh_rug_corner);

// Parlour: 16 tiles
[proc,poh_rug_parlour](coord $base, int $rot, int $tier)
~poh_rug_lay($base, $rot, 2, 2, 3, $tier, ^poh_rug_corner);
~poh_rug_lay($base, $rot, 2, 3, 3, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 2, 4, 3, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 2, 5, 0, $tier, ^poh_rug_corner);
~poh_rug_lay($base, $rot, 3, 2, 2, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 3, 3, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 3, 4, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 3, 5, 0, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 4, 2, 2, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 4, 3, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 4, 4, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 4, 5, 0, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 5, 2, 2, $tier, ^poh_rug_corner);
~poh_rug_lay($base, $rot, 5, 3, 1, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 5, 4, 1, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 5, 5, 1, $tier, ^poh_rug_corner);

// Quest hall: 36 tiles
[proc,poh_rug_quest_hall](coord $base, int $rot, int $tier)
~poh_rug_lay($base, $rot, 1, 1, 3, $tier, ^poh_rug_corner);
~poh_rug_lay($base, $rot, 1, 2, 3, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 1, 3, 3, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 1, 4, 3, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 1, 5, 3, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 1, 6, 0, $tier, ^poh_rug_corner);
~poh_rug_lay($base, $rot, 2, 1, 2, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 2, 2, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 2, 3, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 2, 4, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 2, 5, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 2, 6, 0, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 3, 1, 2, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 3, 2, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 3, 3, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 3, 4, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 3, 5, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 3, 6, 0, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 4, 1, 2, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 4, 2, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 4, 3, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 4, 4, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 4, 5, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 4, 6, 0, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 5, 1, 2, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 5, 2, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 5, 3, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 5, 4, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 5, 5, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 5, 6, 0, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 6, 1, 2, $tier, ^poh_rug_corner);
~poh_rug_lay($base, $rot, 6, 2, 1, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 6, 3, 1, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 6, 4, 1, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 6, 5, 1, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 6, 6, 1, $tier, ^poh_rug_corner);

// Skill hall: 36 tiles
[proc,poh_rug_skill_hall](coord $base, int $rot, int $tier)
~poh_rug_lay($base, $rot, 1, 1, 3, $tier, ^poh_rug_corner);
~poh_rug_lay($base, $rot, 1, 2, 3, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 1, 3, 3, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 1, 4, 3, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 1, 5, 3, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 1, 6, 0, $tier, ^poh_rug_corner);
~poh_rug_lay($base, $rot, 2, 1, 2, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 2, 2, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 2, 3, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 2, 4, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 2, 5, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 2, 6, 0, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 3, 1, 2, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 3, 2, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 3, 3, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 3, 4, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 3, 5, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 3, 6, 0, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 4, 1, 2, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 4, 2, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 4, 3, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 4, 4, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 4, 5, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 4, 6, 0, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 5, 1, 2, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 5, 2, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 5, 3, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 5, 4, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 5, 5, 2, $tier, ^poh_rug_middle);
~poh_rug_lay($base, $rot, 5, 6, 0, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 6, 1, 2, $tier, ^poh_rug_corner);
~poh_rug_lay($base, $rot, 6, 2, 1, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 6, 3, 1, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 6, 4, 1, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 6, 5, 1, $tier, ^poh_rug_side);
~poh_rug_lay($base, $rot, 6, 6, 1, $tier, ^poh_rug_corner);

// =========================================================================== show and remove

// The piece is stored at the anchor, ^poh_rug_anchor - the one tile of these five rooms that
// carries no hotspot in any of them, so a rug and a statue can never want the same slot.
// The tables below are in TEMPLATE coordinates, which start at the zone's own (0,0), so the
// first thing to do is step back from the anchor to that corner. The anchor offset is in
// instance space and does not turn with the room, which is why this subtraction is flat.
[proc,poh_rug_place](coord $spot, int $angle, int $tier)
def_coord $base = movecoord($spot, calc(0 - ^poh_rug_anchor), 0, calc(0 - ^poh_rug_anchor));
def_int $rot = ~poh_ring_room_rot($base);
switch_int (~poh_rug_room($base)) {
    case 5 : ~poh_rug_bedroom($base, $rot, $tier);   // Bedroom
    case 7 : ~poh_rug_chapel($base, $rot, $tier);   // Chapel
    case 2 : ~poh_rug_parlour($base, $rot, $tier);   // Parlour
    case 14 : ~poh_rug_quest_hall($base, $rot, $tier);   // Quest hall
    case 13 : ~poh_rug_skill_hall($base, $rot, $tier);   // Skill hall
}

// Which room this coord is in, as a poh_rooms.enum type.
[proc,poh_rug_room](coord $spot)(int)
if (%poh_instance = null) {
    return(0);
}
def_int $gx = calc(coordx($spot) - coordx(%poh_instance));
def_int $gz = calc(coordz($spot) - coordz(%poh_instance));
return(~poh_room_type(calc(divide($gx, 8) - ^poh_grid_origin), calc(divide($gz, 8) - ^poh_grid_origin)));

// Taking one out. Any tile of the rug is a valid click, so the slot is looked up at the
// anchor. ~poh_furn_relay re-lays the room, which clears the whole rug at once.
[proc,poh_rug_remove]
if (%poh_instance = null | instance_find(loc_coord) ! %poh_instance) {
    return;
}
def_int $gx = calc(coordx(loc_coord) - coordx(%poh_instance));
def_int $gz = calc(coordz(loc_coord) - coordz(%poh_instance));
def_int $rx = calc(divide($gx, 8) - ^poh_grid_origin);
def_int $rz = calc(divide($gz, 8) - ^poh_grid_origin);
def_int $slot = ~poh_furn_at($rx, $rz, ^poh_rug_anchor, ^poh_rug_anchor);
if ($slot < 0) {
    mes("That isn't yours to take out.");
    return;
}
def_int $item = ~poh_furn_item(~poh_furn_get($slot));
def_int $option = ~p_choice2("Take out the <enum(int, string, poh_furn_name, $item)>.", 1, "Leave it.", 2);
if ($option ! 1) {
    return;
}
~poh_furn_set($slot, 0);
~poh_furn_relay($rx, $rz);
mes("You take out the <enum(int, string, poh_furn_name, $item)>.");

// =========================================================================== the triggers

[oploc5,loc_13589] ~poh_rug_remove;
[oploc5,loc_13590] ~poh_rug_remove;
[oploc5,loc_13592] ~poh_rug_remove;
[oploc5,loc_13593] ~poh_rug_remove;
[oploc5,loc_13595] ~poh_rug_remove;
[oploc5,loc_13596] ~poh_rug_remove;
