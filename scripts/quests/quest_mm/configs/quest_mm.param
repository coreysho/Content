// Which mm_transmogrification_* npc a greegree turns its wearer into.
// Declared here rather than reusing an existing param because nothing in the cache links a greegree
// obj to a monkey form - see configs/quest_mm.obj.
[mm_transmog_npc]
type=npc
default=null
