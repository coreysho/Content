// Gunnjorn's [opnpc1,gunnjorn] dialogue was merged into quests/quest_horror/scripts/quest_horror.rs2
// (a script-compiled trigger label can only be declared once globally - having it here too caused
// "[opnpc1,gunnjorn] is already defined"). His original flavour lines are preserved there as the
// not-yet-quest-relevant case, so nothing was lost.
