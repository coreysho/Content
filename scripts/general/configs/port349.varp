// One bit per quest ported from PlagueCityRS's 349 content (2026-09-25). The ports replaced this
// server's own versions of those quests with the originals, whose stage numbers differ, so each
// quest's ~port349_migrate_<quest> translates an old save once and then sets its bit here.
[port349_migrated]
scope=perm
