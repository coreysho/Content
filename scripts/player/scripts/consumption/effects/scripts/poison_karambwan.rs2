// Tai Bwo Wannai Trio (PlagueCityRS 349): a poorly cooked karambwan is the poison, not a meal.
[label,consume_effect_poisonbwan]
sound_synth(eat, 1, 0);
sound_synth(throwup1, 1, 30);
anim(human_eat, 0);
inv_delslot(inv, last_slot);
if(stat(hitpoints) > 5) damage(uid, ^hitmark_poison, 5);
mes("You eat the Karambwan.");
p_delay(2);
mes("It tastes ... painful.");
